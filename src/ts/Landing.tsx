import React, { FC } from 'react';
import '../sass/styles.scss';
import Login from './Login';

const Landing: FC = () => {
  return (
    <div>
      <Login />
    </div>
  );
};

export default Landing;
