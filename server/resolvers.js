const dgraph = require('dgraph-js');
const grpc = require('grpc');

// eslint-disable-next-line no-undef
module.exports = {
  Query: {
    //
  },
  Mutation: {
    //
  },
  Subscription: {
    BoardUpdate(a, b, c, d) {
      debugger;
    },
  },
};
